+ 检查发现竟然图像的文件名排序跟预想的不一样，本以为是1 2 3，但实际上是2 1 3.

+ 可以用单个样本（三个图片）来进行训练，看会不会overfitting，如果不会，
那说明是输入输出的问题而并非网络的问题@柏安，因为网络总能凑成你想要的样子，
比如这一次，实际上用的是2 1来训练，得到3。所以如果我验证时输入2和1，那么
它的确会给我返回3，但在验证的时候我用的是1 3，所以最终输出的肯定时怪怪的，
并且实际上它会给我输出一个很像3的东西。

+ ResNet中的恒等变换处不应该再给它relu

+ v0.3.x 是interp任务
    v0.4.x 是denoise任务
    v0.5.x 是super resolution任务
    v0.6.x 是兼容三个任务

+ v0.7.x 是在v0.6.x版本上选择训练interp任务训练
    v0.8.x 是在v0.6.x版本上选择训练denoise任务训练
    v0.9.x 是在v0.6.x版本上选择训练super resolution任务训练
    v1.0.0 是正式版本(激动)